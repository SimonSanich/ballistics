title = Ballistic Calculator
package.name = s60
package.domain = org.kivy
source.include_exts = py,kv,png,jpg,atlas

# Entry point
source.main = main.py

# Add dependencies (Kivy)
requirements = python3.10, kivy

# Permissions if needed
# android.permissions = INTERNET
log_level = 2